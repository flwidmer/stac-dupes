-- CloudBeaver database connection initialization script
-- This script runs once on CloudBeaver startup and creates a preconfigured connection to the PostGIS database

-- Connection configuration (CloudBeaver uses internal SQL-like syntax for connection setup)
-- Note: CloudBeaver connections are stored in workspace metadata, not direct SQL
-- This is a placeholder for documentation - actual connection setup happens via environment or manual import

-- To preload connections, CloudBeaver can use:
-- 1. Environment variables (handled via Docker Compose)
-- 2. Workspace metadata files (created on first login via web UI)
-- 3. Backup/restore of workspace configuration

-- For production, consider creating a CloudBeaver workspace export with preconfigured connections
